import React from "react";
import {ProfileImageComponent} from './ProfileImageComponent';

// @ts-ignore
export default {
    title: "Components|ProfileImage"
};

export const Default = () => <ProfileImageComponent/>;
