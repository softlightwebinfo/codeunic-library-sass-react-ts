import React from "react";
import {CheckComponent} from './CheckComponent';

// @ts-ignore
export default {
    title: "Components|Check"
};

export const Default = () => <CheckComponent check={true} onClick={() => {
}}/>;
